import { useEffect, useRef, useState } from "react";
import "./MusicPlayer.css";
export default function MusicPlayer({playing}){const audio=useRef(null);const [muted,setMuted]=useState(false);useEffect(()=>{if(!audio.current)return;if(playing)audio.current.play().catch(()=>{});else audio.current.pause()},[playing]);useEffect(()=>{if(audio.current)audio.current.muted=muted},[muted]);return <><audio ref={audio} loop src="/music/music.mp3"/><button className={`music-button ${playing&&!muted?"is-playing":""}`} onClick={()=>setMuted(!muted)} aria-label={muted?"Музыканы қосу":"Музыканы өшіру"}><span>{muted?"♪":"♫"}</span></button></>}
